{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/configuration/checksum/http/v1alpha1/spec/schemas/HostConfig.schema.json",
  "title": "HostConfig",
  "type": "object",
  "description": "HostConfig carries per-host overrides. Unset fields fall through to\n[Config.DefaultChecksumPolicy].",
  "properties": {
    "checksumPolicy": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumPolicy"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumPolicy": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumPolicy",
      "type": "object",
      "description": "ChecksumPolicy is an ordered list of checksum sources evaluated first-match\nwins, plus the behaviour when none yields a checksum, plus an optional\nalgorithm preference used by the access-side digest processor when pinning\nfrom what the source advertises.\n\nStorage semantics differ per side. The input method always downloads and\nrecords SHA-256, regardless of which algorithm verified the transfer. The\naccess-side digest processor records whichever algorithm the source\nadvertises: this is safe because an access references remote bytes and any\nconsumer re-fetches and re-verifies against the same source.\n\nVerification posture is a deployment concern, not a descriptor concern, so\nthis type lives here rather than on the Wget/v1 input spec.",
      "properties": {
        "onMissing": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.OnMissingChecksum",
          "description": "OnMissing controls the behaviour when no source yields a checksum.\nDefaults to \"fail\".",
          "oneOf": [
            {
              "const": "fail"
            },
            {
              "const": "compute"
            }
          ]
        },
        "preferredAlgorithms": {
          "type": "array",
          "description": "PreferredAlgorithms restricts and orders the algorithms the access-side\ndigest processor accepts when pinning. Strongest-preferred first; empty\nmeans [sha256, sha512, sha1, md5]. Ignored on the input side.",
          "items": {
            "type": "string"
          }
        },
        "sources": {
          "type": "array",
          "description": "Sources are the checksum strategies to try, in order.",
          "items": {
            "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumSource"
          }
        }
      },
      "additionalProperties": false
    },
    "ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumSource": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumSource",
      "type": "object",
      "description": "ChecksumSource configures a single checksum-retrieval strategy.",
      "properties": {
        "algorithms": {
          "type": "array",
          "description": "Algorithms restricts which algorithms this source considers (as file\nextensions: sha256, sha512, sha1, md5), strongest-preferred first. Empty\nmeans all supported.",
          "items": {
            "type": "string"
          }
        },
        "headers": {
          "type": "array",
          "description": "Headers lists additional response header names to inspect for httpHeader\nsources, beyond RFC 9530 and x-checksum-*. Algorithm is inferred from the\ntrailing token (e.g. \"x-my-sha256\").",
          "items": {
            "type": "string"
          }
        },
        "type": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumSourceType",
          "description": "Type selects the retrieval strategy.",
          "oneOf": [
            {
              "const": "httpHeader"
            },
            {
              "const": "externalUrl"
            },
            {
              "const": "stream"
            }
          ]
        },
        "url": {
          "type": "string",
          "description": "URL is the absolute checksum URL for externalUrl sources; empty falls\nback to \u003cartifact URL\u003e.\u003cext\u003e. One URL per source: add more sources to\ncover multiple algorithms or hosts."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumSourceType": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumSourceType",
      "type": "string",
      "description": "ChecksumSourceType selects a strategy for obtaining an expected checksum,\nmodelled on Maven's expected-checksum strategies.",
      "oneOf": [
        {
          "description": "ChecksumSourceHTTPHeader reads the checksum from the download response\nheaders (RFC 9530 Content-Digest and x-checksum-*).",
          "const": "httpHeader"
        },
        {
          "description": "ChecksumSourceExternalURL fetches the checksum from a sibling URL\n(Maven's \u003curl\u003e.\u003cext\u003e convention).",
          "const": "externalUrl"
        },
        {
          "description": "ChecksumSourceStream computes the digest from the stream without an\nexternal expected checksum.",
          "const": "stream"
        }
      ]
    },
    "ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.OnMissingChecksum": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "OnMissingChecksum",
      "type": "string",
      "description": "OnMissingChecksum controls policy behaviour when no source yields a checksum.",
      "oneOf": [
        {
          "description": "OnMissingFail aborts with an error. Default when a ChecksumPolicy is set.",
          "const": "fail"
        },
        {
          "description": "OnMissingCompute records the digest computed from the stream, unverified.",
          "const": "compute"
        }
      ]
    }
  }
}