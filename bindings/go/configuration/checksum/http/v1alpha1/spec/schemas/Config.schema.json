{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/configuration/checksum/http/v1alpha1/spec/schemas/Config.schema.json",
  "title": "Config",
  "type": "object",
  "description": "Config is the wire format of the checksum-over-HTTP configuration. It steers\nboth the wget input method and the wget access resource repository through\na single knob.\n\ntype: generic.config.ocm.software/v1\nconfigurations:\n- type: checksum.http.config.ocm.software/v1alpha1\nmode: Prefer\nhosts:\n\"repo.example.com\":\nmode: Require",
  "properties": {
    "hosts": {
      "type": "object",
      "description": "Hosts maps \"host\" or \"host:port\" to per-host overrides; port-qualified\nentries win over bare hostnames.",
      "additionalProperties": {
        "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumPolicy"
      }
    },
    "mode": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumMode",
      "description": "Mode is the default verification behaviour for every wget resource whose\nhost does not match [Config.Hosts]. Defaults to \"Prefer\" when unset.",
      "oneOf": [
        {
          "const": "Require"
        },
        {
          "const": "Prefer"
        },
        {
          "const": "Skip"
        }
      ]
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "oneOf": [
        {
          "const": "checksum.http.config.ocm.software/v1alpha1"
        },
        {
          "deprecated": true,
          "const": "checksum.http.config.ocm.software"
        }
      ]
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumMode": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumMode",
      "type": "string",
      "description": "ChecksumMode selects the checksum-verification posture for a wget resource.\nIt is the sole verification knob: the initial configuration surface is\ndeliberately reduced to a single mode, with room to grow (e.g. early\ntransfer abort on descriptor digest mismatch) without another wire type.\n\nThe zero value means [ChecksumModePrefer].\n\nStorage semantics differ per side. The input method always downloads and\nrecords SHA-256, regardless of which algorithm verified the transfer. The\naccess-side digest processor records whichever algorithm the source\nadvertises: this is safe because an access references remote bytes and any\nconsumer re-fetches and re-verifies against the same source.\n\nVerification posture is a deployment concern, not a descriptor concern, so\nthis type lives here rather than on the Wget/v1 input spec.",
      "oneOf": [
        {
          "description": "ChecksumModeRequire requires verification against a source-advertised\nchecksum (RFC 9530 Content-Digest or x-checksum-*) and fails when none\nis advertised. The access side pins the digest from the advertised\nchecksum without downloading the body; the input side verifies the\ndownloaded bytes against it.",
          "const": "Require"
        },
        {
          "description": "ChecksumModePrefer verifies against a source-advertised checksum when one\nis available and otherwise falls back to computing SHA-256. This is the\ndefault when the mode is unset. On the access side the fallback downloads\nand hashes the body; on the input side the downloaded bytes are recorded\nas SHA-256 without verification.",
          "const": "Prefer"
        },
        {
          "description": "ChecksumModeSkip never consults source-advertised checksums: the body is\ndownloaded and hashed as SHA-256 without verification.",
          "const": "Skip"
        }
      ]
    },
    "ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumPolicy": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumPolicy",
      "type": "object",
      "description": "ChecksumPolicy is a per-host verification override carried in\n[Config.Hosts]. Its only field is the [ChecksumMode]; the top-level default\nlives on [Config.Mode].",
      "properties": {
        "mode": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.configuration.checksum.http.v1alpha1.spec.ChecksumMode",
          "description": "Mode selects the verification behaviour. Defaults to \"Prefer\" when unset.",
          "oneOf": [
            {
              "const": "Require"
            },
            {
              "const": "Prefer"
            },
            {
              "const": "Skip"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    }
  }
}