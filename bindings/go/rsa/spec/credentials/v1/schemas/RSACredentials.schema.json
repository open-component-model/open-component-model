{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/rsa/spec/credentials/v1/schemas/RSACredentials.schema.json",
  "title": "RSACredentials",
  "type": "object",
  "description": "RSACredentials represents typed credentials for RSA signing and verification.",
  "properties": {
    "privateKeyPEM": {
      "type": "string",
      "description": "PrivateKeyPEM holds an inline PEM-encoded RSA private key (PKCS#1 or PKCS#8).\nRequired for signing. See CredentialKeyPrivateKeyPEM for precedence rules."
    },
    "privateKeyPEMFile": {
      "type": "string",
      "description": "PrivateKeyPEMFile is the path to a PEM file containing an RSA private key (PKCS#1 or PKCS#8).\nUsed when CredentialKeyPrivateKeyPEM is absent. See CredentialKeyPrivateKeyPEMFile for full semantics."
    },
    "publicKeyPEM": {
      "type": "string",
      "description": "PublicKeyPEM holds an inline PEM-encoded RSA public key or X.509 certificate chain.\nSee CredentialKeyPublicKeyPEM for full semantics and precedence rules."
    },
    "publicKeyPEMFile": {
      "type": "string",
      "description": "PublicKeyPEMFile is the path to a PEM file containing an RSA public key or X.509 certificate chain.\nUsed when CredentialKeyPublicKeyPEM is absent. See CredentialKeyPublicKeyPEMFile for full semantics."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "oneOf": [
        {
          "const": "RSACredentials/v1"
        },
        {
          "deprecated": true,
          "const": "RSACredentials"
        }
      ]
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    }
  }
}