version: '3'

tasks:
  tmp:
    cmds:
      - mkdir -p tmp
    status:
      - test -d tmp
  build:
    cmds:
      - go build -o manager/registries/componentversionrepository/testdata/test-plugin generic_plugin/main.go
      - go build -o manager/testdata/test-plugin generic_plugin/main.go
  test:
    sources:
      - ./**/*.go
      - ./go.mod
      - ./go.sum
    cmds:
      - go test -v -coverprofile=tmp/coverage.out ./...
    deps:
      - tmp
      - build