{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/transfer/v1alpha1/spec/schemas/HTTPUploaderConfig.schema.json",
  "title": "HTTPUploaderConfig",
  "type": "object",
  "description": "HTTPUploaderConfig is a declarative rule that streams matching resources to a\ncustom HTTP target during transfer. It is carried as an entry inside the central\ngeneric configuration (generic.config.ocm.software/v1), as a sibling of [Config],\nand extracted with [LookupHTTPUploaderConfigs]. Each entry is an independent rule;\nentries are not merged. The config type dedicates it to the HTTP streaming target,\nso the upload request fields are declared inline rather than in a nested stream\nsub-type.\n\ntype: generic.config.ocm.software/v1\nconfigurations:\n- type: http.uploader.transfer.config.ocm.software/v1alpha1\nmatch:\naccessType: Wget/v1\ntargetURL: '${\"https://mytarget.registry.com/uploads\" + url(resource.access.url).path}'\nmethod: PUT",
  "properties": {
    "body": {
      "type": "string",
      "description": "Body is an optional static request body.",
      "contentEncoding": "base64"
    },
    "header": {
      "type": "object",
      "description": "Header carries additional HTTP request headers.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "match": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.transfer.v1alpha1.spec.UploaderMatch",
      "description": "Match selects the resources this uploader applies to."
    },
    "mediaType": {
      "type": "string",
      "description": "MediaType overrides the media type recorded on the uploaded resource; when\nempty it defaults to the source access media type where available."
    },
    "method": {
      "type": "string",
      "description": "Method is the HTTP verb used for the upload request (e.g. PUT)."
    },
    "noRedirect": {
      "type": "boolean",
      "description": "NoRedirect disables following HTTP redirects for the upload request."
    },
    "targetURL": {
      "type": "string",
      "description": "TargetURL is a standalone CEL expression wrapped in ${...} (referencing the\nsource resource via the `resource` alias) that resolves to the upload URL.\nAppend any static query string directly inside the expression."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "oneOf": [
        {
          "const": "http.uploader.transfer.config.ocm.software/v1alpha1"
        },
        {
          "deprecated": true,
          "const": "http.uploader.transfer.config.ocm.software"
        }
      ]
    }
  },
  "required": [
    "type",
    "match",
    "targetURL"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Identity": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "Identity",
      "type": "object",
      "description": "Identity is a map that represents a set of attributes that uniquely identity\narbitrary resources. It is used in various places in Open Component Model to uniquely\nidentity objects such as resources or components.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    },
    "ocm.software.open-component-model.bindings.go.transfer.v1alpha1.spec.UploaderMatch": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "UploaderMatch",
      "type": "object",
      "description": "UploaderMatch selects resources by their access type and, optionally, their\nidentity. A resource matches when its access type matches AccessType and every\nspecified identity constraint (Name, Version, ExtraIdentity) also matches. This lets\nmultiple uploaders target the same access type while routing different resources\nto different upload targets; the first matching uploader (in declaration order)\nwins, so more specific rules should be declared before broader ones.",
      "properties": {
        "accessType": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
          "description": "AccessType is the resource access type this uploader matches (e.g. Wget/v1)."
        },
        "extraIdentity": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Identity",
          "description": "ExtraIdentity optionally restricts the match to resources whose identity\ncontains all of these key/value pairs. When empty, no extra-identity\nconstraint is applied."
        },
        "name": {
          "type": "string",
          "description": "Name optionally restricts the match to resources with this exact name.\nWhen empty, resources of any name match."
        },
        "version": {
          "type": "string",
          "description": "Version optionally restricts the match to resources with this exact\nversion. When empty, resources of any version match."
        }
      },
      "required": [
        "accessType"
      ],
      "additionalProperties": false
    }
  }
}