{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/transfer/v1alpha1/spec/schemas/UploaderConfig.schema.json",
  "title": "UploaderConfig",
  "type": "object",
  "description": "UploaderConfig is a declarative rule that routes matching resources through a\ncustom upload transformer during transfer. It is carried as an entry inside the\ncentral generic configuration (generic.config.ocm.software/v1), as a sibling of\n[Config], and extracted with [LookupUploaderConfigs]. Each entry is an independent\nrule; entries are not merged.\n\ntype: generic.config.ocm.software/v1\nconfigurations:\n- type: uploader.transfer.config.ocm.software/v1alpha1\nmatch:\naccessType: Wget/v1alpha1\nstream:\ntype: HTTPStreaming/v1alpha1\ntargetURL: 'https://mytarget.registry.com/{{.path}}'\nmethod: PUT\nqueryParams: {}",
  "properties": {
    "match": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.transfer.v1alpha1.spec.UploaderMatch",
      "description": "Match selects the resources this uploader applies to."
    },
    "stream": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Raw",
      "description": "Stream is the transformer specification used to upload matched resources.\nIt is kept generic so the uploader stays open to different transformer types;\nthe transformer is selected by Stream's own type (e.g. HTTPStreaming/v1alpha1)."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "oneOf": [
        {
          "const": "uploader.transfer.config.ocm.software/v1alpha1"
        },
        {
          "deprecated": true,
          "const": "uploader.transfer.config.ocm.software"
        }
      ]
    }
  },
  "required": [
    "type",
    "match",
    "stream"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Raw": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Raw",
      "type": "object",
      "description": "Raw is used to hold extensions that dynamically define behavior at runtime",
      "properties": {
        "type": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    },
    "ocm.software.open-component-model.bindings.go.transfer.v1alpha1.spec.UploaderMatch": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "UploaderMatch",
      "type": "object",
      "description": "UploaderMatch selects resources by their access type.",
      "properties": {
        "accessType": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
          "description": "AccessType is the resource access type this uploader matches (e.g. Wget/v1alpha1)."
        }
      },
      "required": [
        "accessType"
      ],
      "additionalProperties": false
    }
  }
}