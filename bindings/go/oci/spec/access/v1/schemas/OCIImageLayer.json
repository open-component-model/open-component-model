{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ocm.software/open-component-model/bindings/go/oci/spec/access/v1/schemas/OCIImageLayer.json",
  "description": "OCIImageLayer describes the access for a local blob as an OCI Layer.\nNote that an OCIImageLayer itself usually needs to be resolved through the manifest\nto determine the mediaType of the Layer.\nTo avoid this lookup necessity to interpret the layer, the mediaType\ncan be set in the OCIImageLayer directly and can be used instead of a manifest lookup.\nNote however, that presence of the layer in OCI is only guaranteed if a Manifest\nis present in the repository that references the layer.\n+k8s:deepcopy-gen:interfaces=ocm.software/open-component-model/bindings/go/runtime.Typed\n+k8s:deepcopy-gen=true\n+ocm:typegen=true\n+ocm:jsonschema-gen=true",
  "title": "OCIImageLayer",
  "type": "object",
  "properties": {
    "digest": {
      "description": "Digest is the digest of the targeted content.",
      "type": "object"
    },
    "mediaType": {
      "description": "MediaType is the media type of the object this schema refers to.",
      "type": "string"
    },
    "ref": {
      "description": "Reference is the oci reference to the OCI repository",
      "type": "string"
    },
    "size": {
      "description": "Size specifies the size in bytes of the blob.",
      "type": "integer"
    },
    "type": {
      "type": "string",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    }
  },
  "required": [
    "type",
    "ref",
    "digest",
    "size"
  ]
}