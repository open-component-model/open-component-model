// Package resource implements the OCM resource repository for the GitHub
// access type: it downloads a GitHub repository's source archive at a pinned
// commit via the GitHub REST API and serves it as a tar archive blob.
package resource
