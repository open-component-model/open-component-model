{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/s3/spec/input/v1/schemas/S3Bucket.schema.json",
  "title": "S3Bucket",
  "type": "object",
  "description": "S3Bucket is the input method specification for a resource that comes from a single\nblob (object) in an S3 or S3-compatible bucket. OCM downloads the object during the\ncomponent construction and stores it as a local blob in the component version. The\ncomponent version therefore does not depend on the bucket after the build. It holds\nthe same fields as the S3Bucket access type.",
  "properties": {
    "bucketName": {
      "type": "string",
      "description": "BucketName is the name of the bucket that holds the object."
    },
    "endpoint": {
      "type": "string",
      "description": "Endpoint is the base endpoint of an S3-compatible store, for example MinIO, Ceph\nor R2. When it is empty, OCM uses AWS S3."
    },
    "mediaType": {
      "type": "string",
      "description": "MediaType is the media type of the referenced object."
    },
    "objectKey": {
      "type": "string",
      "description": "ObjectKey is the key (path) of the object in the bucket."
    },
    "region": {
      "type": "string",
      "description": "Region is the region of the bucket. It is optional. When it is empty, OCM reads\nit from the environment or applies a default. Most custom endpoints ignore it."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "oneOf": [
        {
          "const": "S3Bucket/v1"
        },
        {
          "const": "s3Bucket/v1"
        },
        {
          "deprecated": true,
          "const": "S3Bucket"
        },
        {
          "deprecated": true,
          "const": "s3Bucket"
        }
      ]
    },
    "usePathStyle": {
      "type": "boolean",
      "description": "UsePathStyle puts the bucket in the path instead of in the host. Most self-hosted\nS3-compatible stores need this."
    },
    "version": {
      "type": "string",
      "description": "Version pins one S3 object version (versionId). When it is empty, OCM reads the\nlatest version."
    }
  },
  "required": [
    "type",
    "bucketName",
    "objectKey"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    }
  }
}