{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/wget/spec/input/v1/schemas/Wget.schema.json",
  "title": "Wget",
  "type": "object",
  "description": "Wget describes an input sourced by downloading a resource from an HTTP/S URL\nduring component construction. The downloaded content is stored as a local blob\nin the component version.",
  "properties": {
    "body": {
      "type": "string",
      "description": "Body is the HTTP body to send with the request.",
      "contentEncoding": "base64"
    },
    "checksumPolicy": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.wget.spec.input.v1.ChecksumPolicy",
      "description": "ChecksumPolicy optionally configures how an expected checksum for the\ndownloaded content is obtained and verified when the resource carries no\ndigest of its own. When unset, the digest is computed from the downloaded\nstream without external verification. See [ChecksumPolicy]."
    },
    "header": {
      "type": "object",
      "description": "Header contains HTTP headers to be sent with the request.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "mediaType": {
      "type": "string",
      "description": "MediaType is the media type of the resource with optional format qualifiers."
    },
    "noRedirect": {
      "type": "boolean",
      "description": "NoRedirect disables following HTTP redirects when set to true."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "oneOf": [
        {
          "const": "wget/v1"
        },
        {
          "const": "Wget/v1"
        },
        {
          "deprecated": true,
          "const": "wget"
        },
        {
          "deprecated": true,
          "const": "Wget"
        }
      ]
    },
    "url": {
      "type": "string",
      "description": "URL is the HTTP endpoint to download the resource from."
    },
    "verb": {
      "type": "string",
      "description": "Verb is the HTTP method to use (GET, POST, etc.). Defaults to GET."
    }
  },
  "required": [
    "type",
    "url"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    },
    "ocm.software.open-component-model.bindings.go.wget.spec.input.v1.ChecksumPolicy": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumPolicy",
      "type": "object",
      "description": "ChecksumPolicy is an ordered list of checksum sources evaluated first-match\nwins, plus the behaviour when none yields a checksum. The digest recorded on\nthe resource is always SHA-256; a source may verify the transferred bytes\nagainst a different algorithm (e.g. SHA-1 from a Maven repository) without\nchanging the stored algorithm.",
      "properties": {
        "onMissing": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.wget.spec.input.v1.OnMissingChecksum",
          "description": "OnMissing controls the behaviour when no source yields a checksum. Defaults\nto \"fail\".",
          "oneOf": [
            {
              "const": "fail"
            },
            {
              "const": "compute"
            }
          ]
        },
        "sources": {
          "type": "array",
          "description": "Sources are the checksum strategies to try, in order. The first that yields\nan expected checksum is used to verify the download.",
          "items": {
            "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.wget.spec.input.v1.ChecksumSource"
          }
        }
      },
      "additionalProperties": false
    },
    "ocm.software.open-component-model.bindings.go.wget.spec.input.v1.ChecksumSource": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumSource",
      "type": "object",
      "description": "ChecksumSource configures a single checksum-retrieval strategy within a\n[ChecksumPolicy].",
      "properties": {
        "algorithms": {
          "type": "array",
          "description": "Algorithms restricts which checksum algorithms this source considers, given\nas file extensions (sha256, sha512, sha1, md5), strongest-preferred first.\nWhen empty, all supported algorithms are considered.",
          "items": {
            "type": "string"
          }
        },
        "headers": {
          "type": "array",
          "description": "Headers lists additional response header names to inspect for httpHeader\nsources, beyond the standard RFC 9530 and x-checksum-* headers. The\nalgorithm is inferred from a trailing token (e.g. \"x-my-sha256\").",
          "items": {
            "type": "string"
          }
        },
        "type": {
          "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.wget.spec.input.v1.ChecksumSourceType",
          "description": "Type selects the retrieval strategy.",
          "oneOf": [
            {
              "const": "httpHeader"
            },
            {
              "const": "externalUrl"
            },
            {
              "const": "stream"
            }
          ]
        },
        "url": {
          "type": "string",
          "description": "URL is a CEL expression that resolves the sibling checksum URL for\nexternalUrl sources. It MUST be a single standalone expression wrapped in\n${…} and returns a string. Available variables:\n- resource: the wget input under construction, with fields\nurl/mediaType/verb/header/noRedirect and parsed url.path/host/scheme\n- ext: the algorithm's file extension (e.g. \"sha256\", \"sha1\")\n- alg: the algorithm's OCM name (e.g. \"SHA-256\")\nEmpty defaults to `${resource.url + \".\" + ext}` (Maven's convention).\n\nExample: '${\"https://mirror.example/checksums/\" + ext + resource.url.path}'."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "ocm.software.open-component-model.bindings.go.wget.spec.input.v1.ChecksumSourceType": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "ChecksumSourceType",
      "type": "string",
      "description": "ChecksumSourceType selects a strategy for obtaining an expected checksum,\nmodelled on Maven's expected-checksum strategies.",
      "oneOf": [
        {
          "description": "ChecksumSourceHTTPHeader reads the checksum from the download response\nheaders (\"Remote Included\"): the RFC 9530 Content-Digest field and the\nnon-standard x-checksum-* family.",
          "const": "httpHeader"
        },
        {
          "description": "ChecksumSourceExternalURL fetches the checksum from a sibling URL\n(\"Remote External\"), e.g. \u003curl\u003e.sha256.",
          "const": "externalUrl"
        },
        {
          "description": "ChecksumSourceStream computes the digest from the downloaded stream without\nan external expected checksum.",
          "const": "stream"
        }
      ]
    },
    "ocm.software.open-component-model.bindings.go.wget.spec.input.v1.OnMissingChecksum": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "generated by the ocm schema generation tool",
      "title": "OnMissingChecksum",
      "type": "string",
      "description": "OnMissingChecksum controls what happens when a [ChecksumPolicy] is configured\nbut none of its sources yields an expected checksum.",
      "oneOf": [
        {
          "description": "OnMissingFail aborts the input with an error when no source yields a checksum.\nThis is the default when a ChecksumPolicy is set.",
          "const": "fail"
        },
        {
          "description": "OnMissingCompute falls back to computing the digest from the stream without\nexternal verification when no source yields a checksum.",
          "const": "compute"
        }
      ]
    }
  }
}