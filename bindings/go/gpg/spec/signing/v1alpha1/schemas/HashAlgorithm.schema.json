{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/gpg/spec/signing/v1alpha1/schemas/HashAlgorithm.schema.json",
  "title": "HashAlgorithm",
  "type": "string",
  "description": "HashAlgorithm names the hash function used when signing digest bytes.",
  "oneOf": [
    {
      "const": "SHA-256"
    },
    {
      "const": "SHA-384"
    },
    {
      "const": "SHA-512"
    }
  ]
}