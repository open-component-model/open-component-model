# OCM Component Version Settings
# Used by component constructors to inject version information

VERSION: 1.0.0
POSTGRES_VERSION: "16-alpine"

# Registry configuration for air-gapped scenarios
LOCAL_REGISTRY: localhost:5001
REGISTRY_NAMESPACE: ocm/conformance

# Component metadata
COMPONENT_PROVIDER: acme.org
COMPONENT_NAMESPACE: acme.org/sovereign

# Build configuration
BUILD_PLATFORM: linux/amd64,linux/arm64
BUILD_CONTEXT: .

# Test configuration  
TEST_TIMEOUT: 300s
HEALTH_CHECK_TIMEOUT: 180s