apiVersion: v1
kind: Service
metadata:
  name: {{ include "sovereign-notes.fullname" . }}
  labels:
    {{- include "sovereign-notes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sovereign-notes.selectorLabels" . | nindent 4 }}