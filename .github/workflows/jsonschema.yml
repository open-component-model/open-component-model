name: JSON Schema
on:
  push:
    branches:
    - main
    paths:
    - '**/schemas/*.schema.json'
  pull_request:
    branches:
      - main
    paths:
      - '**/schemas/*.schema.json'

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
    - name: Setup JSON Schema
      uses: sourcemeta/jsonschema@96d8ce7a4e6ddf5ef86b9d90efbb250ed02da76a # v13.0.0
    - name: Lint JSON Schemas
      run: |
        jsonschema lint $(git ls-files '**/schemas/*.schema.json') \
          --verbose \
          --strict \
          --exclude additional_properties_default \
          --exclude enum_with_type