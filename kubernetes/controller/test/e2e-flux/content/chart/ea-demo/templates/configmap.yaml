apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-ea-helm-proof
data:
  message: {{ .Values.message | quote }}
  chart: {{ .Chart.Name }}-{{ .Chart.Version }}
  release: {{ .Release.Name }}
